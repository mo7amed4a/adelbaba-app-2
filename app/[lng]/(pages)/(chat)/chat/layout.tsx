import ChatDrawer from "@/components/template/ChatDrawer";

export default function ChatLayout({
  children,
}: {
  children: React.ReactNode;
}) {
//   return <div className="mt-16">{children}</div>;
  return <div className="mt-16">
    <ChatDrawer >
        wfkodm
    </ChatDrawer>
  </div>;
}
